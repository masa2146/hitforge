---
description: Run a market scan — research trending mobile-game mechanics and produce scored, non-repeating concept cards. Pass --manual to supply sources yourself when web search is unavailable.
---

Invoke the `market-scan` skill (`${CLAUDE_PLUGIN_ROOT}/skills/market-scan/SKILL.md`) and follow it exactly.

Arguments: `$ARGUMENTS` (e.g. `--manual` to run manual mode).

When finished, update `docs/pipeline-state.md`.
